import 'package:flutter/material.dart';

class Archeiv extends StatelessWidget {
  const Archeiv({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Archeiv task",
        style: TextStyle(fontSize: 25,fontWeight: FontWeight.bold),
      ),
    );;
  }
}
